# CONTINUITY.md

This file records where the wedding website build stands so future Codex sessions can continue without rediscovering basics.

## Current Objective

Implement the Cloudflare D1 RSVP system:

- store RSVP data in Cloudflare D1;
- provide a multilingual guest RSVP form;
- provide a private admin/viewing dashboard;
- send admin notification and guest confirmation emails through Cloudflare Email Sending;
- verify the protected guest/admin flow end to end before deployment.

## Known Inputs

- Main specification: `doc/plans/wedding_website_build_spec.md`
- Visual references: `doc/visuals`
- Draft source site: `https://gdagomcarta.minted.us`
- GitHub repo: `https://github.com/ManfrediC/wedding-website`

## Product Decisions To Preserve

- Wedding date: Friday, 11 June 2027.
- Ceremony: Kirche St. Peter, Zurich.
- Reception and party: Hotel Sonne, Küsnacht.
- Languages: English, Italian, German.
- Privacy: site-wide password protection, except for the public animated invitation at `/petri-turicensis-vi-mmxxvii/`.
- RSVP direction: Cloudflare D1 custom RSVP with a private admin/viewing system. Supersede responses by normalised email; no invitation codes; no guest-side editing links; omit travel/accommodation fields.
- Location is Zurich and Küsnacht. Cambridge content from the draft site does not apply.

## Open Decisions

- Shared wedding password.
- Production RSVP admin password and admin cookie secret.
- Cloudflare Email Sending domain onboarding/provider health for `gabyandmanfredi.net`.
- RSVP opening date and deadline.
- Final hero image.
- Exact ceremony, reception, and late-night transport times.
- Hotel Sonne direct-booking discount is confirmed; other accommodation room blocks and booking codes remain TBD.
- Gift wording.
- Wedding-week contact.

## Checkpoint Log

- 2026-08-24: Released the high-resolution Turicensis invitation in commits `4b35dcb` and `c40730b`, pushed them to `origin/master`, and confirmed Cloudflare Pages production deployment `60b54854-b027-4c41-b293-78fc34c9f09d` became active. The first live check caught the new external WebPs being intercepted by password middleware; the corrective commit exposes only the exact 1320px and 2640px invitation asset paths and adds protected/live regression coverage. The custom domain now returns both files with HTTP 200 and `image/webp`, and each downloaded file matches its committed SHA-256 bytes. `npm run smoke:live` passed against `https://gabyandmanfredi.net`, including the public invitation assets, password hand-off, authenticated site flow, and 0px desktop/mobile horizontal overflow; screenshots: `tmp/live-protected-smoke-2026-08-24T18-22-54-032Z-desktop.png` and `tmp/live-protected-smoke-2026-08-24T18-22-54-032Z-mobile.png`. The private Proof 3 PDF remains untracked and was not pushed or deployed.

- 2026-08-24: Rebuilt the public Turicensis ceremony invitation from page 4 of the supplied Proof 3 PDF as metadata-free, sRGB lossless WebP assets at 1320x2040 and 2640x4080. The exported artwork contains only the 11:17 invitation face and retains the proof's vector-derived typography while correcting the ceremony line to `AT TWO O'CLOCK IN THE AFTERNOON`; the private source PDF remains untracked and is not copied into the build. Replaced the embedded 1200x1852 PNG with a responsive external image and changed the envelope animation so its opening transition may scale, but its settled state uses a real rounded CSS width, translation-only positioning and `will-change: auto`; resize, replay and reduced-motion paths recalculate or reset the state. Left the reception card, suite, crest, copy, colours and navigation unchanged. Verified both WebPs have exact dimensions, RGB `VP8L` lossless encoding, no EXIF/ICC/XMP chunks, and OCR contains no QR code, password, private/full street address, proof branding, `HALF PAST` or `14:30`; `npm run check`, `npm run build` and `git diff --check` passed; focused Firefox desktop/narrow invitation E2E passed (`4 passed`); and the settled 1365x900 and 390x844 captures were visually reviewed with no horizontal overflow. No commit or deployment was performed.

- 2026-08-24: Released the restored main-site `#415531` lettering in commit `0a3ef09`, pushed it to `origin/master`, and confirmed the Cloudflare Pages Git deployment on `https://gabyandmanfredi.net`. Production no longer serves `#858676`, still serves the Turicensis-matched `#EBEBEB` photo-band background, and leaves the public Turicensis page unchanged. `npm run smoke:live` passed with 0px desktop/mobile horizontal overflow; screenshots: `tmp/live-protected-smoke-2026-08-24T14-04-20-690Z-desktop.png` and `tmp/live-protected-smoke-2026-08-24T14-04-20-690Z-mobile.png`.

- 2026-08-24: Restored main-site green lettering from `#858676` to `#415531` at the user's request. Retained the Turicensis-matched `#EBEBEB` photo-band background and left `public/petri-turicensis-vi-mmxxvii/index.html` unchanged. Verified with `git diff --check` (line-ending warnings only), `npm run check`, and `npm run build`. No deployment was performed at this checkpoint.

- 2026-08-24: Released the main-site Turicensis colour harmonisation in commit `a6bc8fc`, pushed it to `origin/master`, and confirmed the Cloudflare Pages Git deployment on `https://gabyandmanfredi.net`. Production serves `#EBEBEB` for the photo band and `#858676` for main-site green lettering while retaining `#415531` for structural accents; the public Turicensis page remains unchanged. `npm run smoke:live` passed with 0px desktop/mobile horizontal overflow; screenshots: `tmp/live-protected-smoke-2026-08-24T13-52-34-423Z-desktop.png` and `tmp/live-protected-smoke-2026-08-24T13-52-34-423Z-mobile.png`.

- 2026-08-24: Harmonised the main wedding site with the Turicensis invitation without changing the public Turicensis page. Replaced the photo-band `#EFE4D2` background with the invitation ornament's sampled `#EBEBEB`, changed main-site green lettering to `#858676`, and retained `#415531` as the darker structural colour for buttons, borders, form accents, and the footer. Verified with `git diff --check` (line-ending warnings only), `npm run check`, `npm run build`, targeted source/build colour scans, and confirmation that `public/petri-turicensis-vi-mmxxvii/index.html` is unchanged. No browser visual review, commit, or deployment was performed.

- 2026-08-24: Released the invitation-aligned palette and refined password-page scale in commit `b3cbd9b`, pushed it to `origin/master`, and confirmed the Cloudflare Pages Git deployment on `https://gabyandmanfredi.net`. Production serves the new `#415531` palette and the desktop welcome-shell `scale(0.9)` rule. `npm run smoke:live` passed against the custom domain with 0px desktop/mobile horizontal overflow; screenshots: `tmp/live-protected-smoke-2026-08-24T10-51-40-409Z-desktop.png` and `tmp/live-protected-smoke-2026-08-24T10-51-40-409Z-mobile.png`.

- 2026-08-24: Reduced the desktop password-page composition to 90% of its former rendered size and reduced the decorative-frame inset by the same proportion, matching the supplied Firefox 90% reference at normal browser zoom. The existing mobile presentation remains unscaled at widths up to 700px. Verified with `git diff --check` (line-ending warnings only), `npm run check`, `npm run build`, focused Firefox desktop/narrow welcome and root E2E (`6 passed`), computed Firefox readback at 1877x837 (0.9 shell scale, 25.2px frame inset, 0px horizontal overflow) and 390x844 (1.0 shell scale, 0px horizontal overflow), and targeted desktop/mobile visual review. Existing unrelated palette, artwork, QR, specification, and continuity edits were preserved. No commit or deployment was performed.

- 2026-08-24: Released the approved main-site typography in commit `d454939`: Cormorant Garamond now replaces Georgia for display text, including the homepage couple's names at weight 500, and Montserrat replaces the mixed Inter/system stack for body copy, navigation, controls, and long-form guest information. Uppercase navigation and labels use `0.08em` tracking; Pinyon Script is not used on the production site. Verified with `npm run check`, `npm run build`, focused Chrome desktop/mobile typography E2E (`2 passed`), protected RSVP alignment and narrow-overflow E2E (`4 passed`), `npm run smoke:protected`, desktop/mobile hero visual review, `npm run smoke:live`, and authenticated live computed-style checks at 1440x900 and 390x844 (Cormorant Garamond 500, Montserrat body, 0px overflow). The existing unrelated palette, artwork, QR, specification, and continuity edits remained outside the implementation commit.

- 2026-08-23: Aligned the wedding website with the animated invitation's palette. Replaced the deep green `#31483D` with `#415531`, including matching translucent borders/shadows, favicon, placeholder SVG artwork, QR preview source, and build specification. Changed the password landing page from its warmer ivory/champagne treatment to a restrained gradient centred on the invitation's `#EDEAE1`, with nearby `#F2F0EA` and `#E8E5DC` stops. Verified with `git diff --check` (line-ending warnings only), `npm run check`, `npm run build`, `npm run smoke:protected` (desktop/mobile overflow 0), source/build searches for the retired green, computed desktop/mobile welcome-gradient readback, and targeted desktop/mobile visual review. No commit or deployment was performed.

- 2026-08-23: Released the public animated invitation by pushing commit `cedabec` to `origin/master`. Cloudflare Pages production now serves `/petri-turicensis-vi-mmxxvii/` with HTTP 200 and `X-Robots-Tag: noindex, nofollow`; the bare route redirects canonically to the trailing-slash route instead of the password gate. Live `npm run smoke:live` passed against `https://gabyandmanfredi.net`, covering the invitation's closed initial state, reload restart, `/welcome/` password hand-off, authenticated return behaviour, existing multilingual protected pages, and 0px desktop/mobile horizontal overflow. Screenshots: `tmp/live-protected-smoke-2026-08-23T20-40-32-772Z-desktop.png` and `tmp/live-protected-smoke-2026-08-23T20-40-32-772Z-mobile.png`.

- 2026-08-23: Integrated the supplied animated invitation at the public route `/petri-turicensis-vi-mmxxvii/` while keeping `/welcome/` and the rest of the wedding website password-protected. Removed the displayed password, browser-extension injections, returning-visitor persistence, query-token forwarding, and hard-coded deployment origin; the same-origin continuation now goes to `/welcome/`, and the animation restarts on every reload or later visit. Preserved replay, reduced-motion support, CSP, and `noindex`; production middleware and the protected preview allow only the exact bare and trailing-slash invitation paths. Added focused Playwright and protected-smoke coverage. Verified with `git diff --check` (line-ending warnings only), `npm run check`, `npm run build`, focused Chrome desktop/mobile invitation E2E (`4 passed`), `npm run smoke:protected` (desktop/mobile overflow 0), source/build integrity and forbidden-content scans, and visual review at 1440×900 and 390×844. The scope watchdog and adversarial reviewer both passed the implementation with no findings. No deployment or commit was performed.

- 2026-07-25: Released the RSVP desired-menu update and the pending multilingual guest-information copy. Pushed commits `23abdc3` and `9a4d224` to `origin/master`; Cloudflare Pages Git deployment `6aefff1f` built production from `9a4d224`. The live protected-site smoke passed against `https://gabyandmanfredi.net` with 0px desktop/mobile horizontal overflow, and direct live Chrome readback confirmed `Desired menu`/`Meat`/`Allergies and dietary requirements`, `Scelta del menu`/`Carne`/`Allergie ed esigenze alimentari`, and `Essenswunsch`/`Fleisch`/`Allergien und Ernährungsanforderungen`. All three languages retained aligned desktop menu/allergy controls and zero label or page overflow at 1200px and 390px widths. Screenshots: `tmp/live-protected-smoke-2026-07-25T20-48-06-250Z-desktop.png` and `tmp/live-protected-smoke-2026-07-25T20-48-06-250Z-mobile.png`.

- 2026-07-25: Expanded the English, Italian, and German ZVV ticket guidance to explain zone coverage for Zurich Airport, Uetliberg, and selected short boat routes, and clarified when the Zürich Card is useful. Added sun-protection guidance to the multilingual boat-transfer FAQ. Verified as part of the RSVP release checkpoint with `npm run check`, `npm run build`, and `npm run test:rsvp:e2e` (`9 passed`).

- 2026-07-25: Replaced the per-guest RSVP dietary selector with a desired-menu selector in English, Italian, and German. The default option now stores `Meat`/`Carne`/`Fleisch`; allergy fields, admin details, CSV headers/details, notification wording, and confirmation emails now use the corresponding menu and allergy/dietary-requirement terminology. Preserved the existing API and D1 column names for compatibility, and added idempotent migration `0003_replace_none_menu_with_meat.sql` to update exact legacy `None` values in per-guest JSON and rebuild the derived menu summary without changing timestamps or revision counts. The migration passed twice against a local D1 fixture containing meat, vegetarian, custom, and allergy data. Rebalanced desktop guest columns and label spacing for the longer multilingual labels while retaining a compact single-column mobile layout. Verified with `node --check functions/_lib/rsvp.js`, `npm run check`, `npm run build`, `npm run test:rsvp:e2e` (`9 passed`), and rendered English/Italian/German desktop and mobile screenshots in `tmp/rsvp-menu-*.png` (0px horizontal overflow; desktop menu/allergy controls aligned exactly). The in-app Browser listed no available browser, so the visual pass used the installed local Chrome through Playwright. After explicit production approval, recorded the current D1 Time Travel bookmark without displaying it, confirmed exactly two legacy values across the two existing responses, applied migration `0003`, and verified that both values became `Meat`, no targeted `None` values remain, and IDs, timestamps, and revision counts are unchanged.

- 2026-07-10: Added Mama Shelter Zurich to the Stay/Accommodation page as a more affordable Oerlikon option, with its official website and guidance that Zürich Oerlikon is one stop from Zurich Airport on many direct trains and has direct connections to Zürich HB and Küsnacht ZH. Added corresponding Italian and German draft copy plus a focused multilingual E2E test. Removed the private civil-ceremony time from the multilingual Schedule detail and timeline, made timeline times optional, and added regression coverage confirming the omitted time. Expanded protected RSVP coverage for phone-code and dietary choices, attendance-dependent requirements, repeated guest controls, and the current privacy/update copy. Repaired stale cross-browser E2E expectations for the OpenStreetMap label, current travel/guide/schedule copy, Luton and UK passport links, and German visa wording. Offline autoreview found no remaining defects or risk hints and used no model/API/network reviewer. Verified with `npm run check`, `npm run build`, full `npm run test:e2e` (`136 passed`, `72 RSVP tests intentionally skipped and covered by the protected runner`), `npm run test:rsvp:e2e` (`9 passed`), and in-app Browser inspection at 1440px desktop and 390px mobile widths (Mama Shelter copy/link present; 0px horizontal overflow).
- 2026-07-10: Follow-up Stay-page wording now explicitly recommends Mama Shelter Zurich as a more affordable and well-connected option. Added structured rich-text support for bold section-item text and used it for the primary Hotel Sonne, OXEN, Mama Shelter, and Caffètino-Vino accommodation names in English, Italian, and German. Verified with `npm run check`, `npm run build`, focused multilingual Stay E2E across all configured browser profiles (`8 passed`), and rendered desktop/mobile inspection (0px horizontal overflow).

- 2026-07-02: Updated the Travel Advice page after the latest English edits. Under "From Zurich Airport", removed the separate SBB Mobile link box and moved the SBB Mobile app link into the first list item; reduced the Zurich wedding map display to 75% width while keeping the SVG asset crisp; harmonised the edited Travel copy in Italian and German; and kept the compact link-grid treatment consistent across all Travel link lists. Verified with `git diff --check` (line-ending warnings only), `npm run check`, `npm run build`, rendered-output searches for the English/Italian/German airport sections and reduced map class, and live `npm run smoke:live` against `https://gabyandmanfredi.net` (desktop/mobile overflow 0; screenshots `tmp/live-protected-smoke-2026-07-02T14-23-57-737Z-desktop.png` and `tmp/live-protected-smoke-2026-07-02T14-23-57-737Z-mobile.png`). Code commit `3a2c5ce` was pushed to `origin/master` and deployed to Cloudflare Pages preview `https://5cc0958a.wedding-website-2ng.pages.dev`.

- 2026-06-02: Implemented `doc/plans/2026-06-02_docx_notes_additions_plan.md` plus the follow-up hero readability request. Updated English, Italian, and German draft schedule/FAQ/content copy with confirmed guest-seated time `1.40pm`, boat boarding at Quai 6, Bürkliplatz, Zürich by `3.45pm`, `4pm` boat departure to Küsnacht, comfortable-shoes/umbrella guidance, direct Hotel Sonne reception booking discount of approximately 10% for Dago-Carta wedding guests, refined UK rail/Zurich HB-to-Küsnacht ZH guidance, and concise Zürich sightseeing additions including Fraumünster, Giacometti-Halle, Rapperswil, Rheinfall, and Einsiedeln. Changed the homepage hero copy and secondary CTA text/border to charcoal for readability over the photo, and updated E2E assertions for the hero colour, confirmed boat details, Hotel Sonne discount, refreshed travel copy, localised sightseeing headings, and corrected `Kunsthaus Zürich` alt text. Verified with spelling/wording searches for `towards Küsnacht`, `Frauen*`, `Reinfall`, `Künsthaus`, `Rococco`, and `S24`; `git diff --check` showed only line-ending warnings; `npm run check`, `npm run build`, targeted `npm run test:e2e -- tests/e2e/site.spec.ts --grep "Italian and German pages have localised core content"` (8 passed), and full `npm run test:e2e` (128 passed, 64 skipped) passed. The in-app Browser backend listed no available browsers, so local visual review used Playwright with installed Chrome; screenshots saved in `tmp/visual-review/wedding-home-desktop.png`, `wedding-home-mobile.png`, `wedding-schedule-desktop.png`, `wedding-stay-desktop.png`, `wedding-things-desktop.png`, and `wedding-things-mobile.png`. Visual/style assertions confirmed hero text colour `rgb(37, 37, 37)`, no detected text overflow on reviewed pages, schedule Quai 6/boarding copy present, Hotel Sonne discount copy present, and Rheinfall copy present. Italian and German remain draft translations pending fluent review.

- 2026-05-31: Updated the Stay/Accommodation page with the OXEN rooms URL under the Küsnacht option, noting that some OXEN rooms use shared bathrooms. Added English, Italian, and German guidance that towns along the S6/S16 line, or places near Zürich Stadelhofen or Zürich Tiefenbrunnen, can be convenient accommodation bases for reaching Küsnacht. Added E2E assertions for the new English/Italian/German Stay copy and OXEN room links. Verified with `npm run check`, `npm run build`, focused `npm run test:e2e -- tests/e2e/site.spec.ts --project=chrome-desktop --grep "English pages include requested travel and contact details|Italian and German stay copy is localised and cleanly encoded" --reporter=list --output=tmp/stay-accommodation-e2e` (2 passed), protected-preview Chrome screenshots (`tmp/stay-accommodation-en-desktop.png`, `tmp/stay-accommodation-it-desktop.png`, `tmp/stay-accommodation-de-desktop.png`, `tmp/stay-accommodation-de-mobile.png`), and desktop/mobile overflow checks at 0px. Commit `e359b66` was pushed to `origin/master`; a network-approved live Chrome check on `https://gabyandmanfredi.net/en/stay/` confirmed the OXEN `/zimmer` link, shared-bathroom note, S6/S16/Stadelhofen/Tiefenbrunnen guidance, 0px desktop overflow, and saved `tmp/live-stay-accommodation-en-desktop.png`.
- 2026-06-01: Implemented the user-ticked AI-writing copy scan from `doc/notes/2026-06-01_ai_writing_copy_scan_ticked.html`. Updated the approved English source phrases and corresponding Italian/German draft copy across schedule, travel airport transfer, map, accommodation, things-to-do intro/link/museums, Switzerland guide intro/money, FAQ, gifts, contact, RSVP privacy, and exchange-rate widget provenance/loading text. Left unticked audit rows unchanged, including similar remaining schedule/FAQ phrasing that was not selected. Verified with `npm run check` and `npm run build`; no browser visual pass was run because the selected changes were copy-only and did not alter layout or controls.
- 2026-05-31: Fixed the German RSVP guest-row label overlap by changing the per-guest dietary label from the long `Ernährungsanforderungen` to `Essenswunsch` and adding defensive wrapping constraints for RSVP labels. Added E2E coverage that checks RSVP labels stay inside their fields across English, Italian, and German at desktop width. Reproduced the original overlap in local protected preview before the fix (`tmp/de-rsvp-before-overlap.png`), then verified no overflowing labels and a clean German screenshot after the fix (`tmp/de-rsvp-after-overlap.png`). Verified with `npm run test:rsvp:e2e` (8 passed), `npm run check`, `npm run build`, and `git diff --check` (line-ending warnings only). Commit `62ff66e` was pushed to `origin/master`; a network-approved live Chrome check on `https://gabyandmanfredi.net/de/rsvp/` confirmed `Essenswunsch` is deployed, no German guest labels overflow, and saved `tmp/live-de-rsvp-after-overlap.png`.
- 2026-05-31: Pushed RSVP contact/admin commits `d434df2` and `b67abad` to `origin/master`, making `master` current on GitHub and triggering the Cloudflare Pages Git deployment. Verified the live custom domain with network-approved `npm run smoke:live` against `https://gabyandmanfredi.net` (desktop/mobile overflow 0; screenshots `tmp/live-protected-smoke-2026-05-31T18-55-16-118Z-desktop.png` and `tmp/live-protected-smoke-2026-05-31T18-55-16-118Z-mobile.png`). Also ran a focused live Chrome DOM check using the local site password without printing secrets: `/en/rsvp/` has neither attendance option pre-selected, `phone_number` is required, the country-code selector exists, `address` is optional, the contact-if-needed privacy copy is present, and `/admin/rsvp/` no longer includes the Notification table header, filter option, or summary text. Cloudflare Pages deployment-list readback via Wrangler is still blocked by current token permissions (`Authentication error [code: 10000]`), so live verification was performed externally through the custom domain.
- 2026-05-31: Applied the remote Cloudflare D1 migrations to `wedding_rsvp` using Wrangler 4.95.0 with `npx wrangler d1 migrations apply wedding_rsvp --remote` and `CLOUDFLARE_ACCOUNT_ID=43b7bcd0ac30cffda3632878e83bd36d`. Wrangler applied/registered `0001_create_rsvp_responses.sql` and `0002_add_rsvp_contact_fields.sql`; the `0001` migration is idempotent because it uses `CREATE TABLE IF NOT EXISTS`/`CREATE INDEX IF NOT EXISTS`. Verified with remote `PRAGMA table_info(rsvp_responses)` that `phone_number` and `address` now exist as `TEXT NOT NULL DEFAULT ''`, and verified `d1_migrations` contains both migration names with `applied_at` `2026-05-31 18:51:46`. No RSVP data writes or deletes were performed.
- 2026-05-31: Updated the RSVP contact flow: attendance is no longer pre-selected, "Will you be able to join us?", name, email, and phone are compulsory, and each RSVP submission now collects one optional multi-line address plus one required phone number with a compact country-code selector and light server-side phone sanity validation. The privacy copy now says the information may be used to contact guests if needed. RSVP storage, admin API, CSV export, local protected-preview storage, admin/detail view, and admin/guest email summaries now carry `phone_number` and `address`; D1 migration `migrations/0002_add_rsvp_contact_fields.sql` adds the new columns. The admin dashboard no longer displays the Notification column, notification filter, notification summary, or notification detail field, while backend notification tracking remains internal. Italian and German RSVP copy and E2E assertions were updated. Verified with `node --check functions/_lib/rsvp.js`, `node --check scripts/serve-protected-preview.mjs`, `npm run check`, `npm run build`, `npm run test:rsvp:e2e` (8 passed), protected-preview Chrome screenshots/DOM checks for desktop RSVP, mobile RSVP, and admin (`tmp/rsvp-contact-desktop.png`, `tmp/rsvp-contact-mobile.png`, `tmp/rsvp-admin-contact-desktop.png`; zero horizontal overflow), and `npm run test:e2e -- --project=chrome-desktop --reporter=list --output=tmp/full-e2e-contact-results` (16 passed, 8 RSVP specs intentionally skipped in the unprotected runner and covered by `test:rsvp:e2e`). The in-app Browser connector listed no available browser, so the visual pass used local Chrome through Playwright.
- 2026-05-31: Finalised the Cloudflare Pages hosting configuration for `gabyandmanfredi.net`: added and activated `www.gabyandmanfredi.net`, kept the apex CNAME proxied and set the `www` CNAME DNS-only to satisfy Pages verification, persisted `RSVP_NOTIFICATION_FROM=rsvp@gabyandmanfredi.net` in `wrangler.jsonc`, committed and pushed `2454dc3`, and confirmed deployment `d2cb0183-d53d-4b2a-9d3f-b231460f9a1b` succeeded with both apex and `www` aliases. Verified with `npm run check`, `npm run build`, network-approved `npm run smoke:live` against both `https://gabyandmanfredi.net` and `https://www.gabyandmanfredi.net`, and a production RSVP smoke that stored a test RSVP, loaded the admin dashboard, verified admin API/CSV data, and deleted the test row. Remaining blocker: Cloudflare Email Sending is not yet delivering; the RSVP smoke returned `notification_status=failed` with `Cloudflare Email Sending returned HTTP 500 for admin notification`, and a direct MCP Email Sending send call returned `10002: email.sending.error.internal_server`. Cloudflare Email Sending subdomain/onboarding MCP endpoints currently fail in the wrapper with `Cannot read properties of undefined (reading 'map')`; DNS readback shows no `cf-bounce`, DKIM, DMARC, or MX email-service records. Production D1 was left clean with zero `Codex Hosting Smoke` rows.
- 2026-05-31: Updated the repo-side live smoke target and hosting instructions to use the selected domain `gabyandmanfredi.net` instead of the earlier Pages.dev or `.wedding` placeholders. Verified with `npm run check`, `npm run build`, and network-approved `npm run smoke:live` against `https://gabyandmanfredi.net` (Chrome, desktop/mobile overflow 0; screenshots in `tmp/live-protected-smoke-2026-05-31T16-10-51-680Z-*`). Local `env/cloudflare.env` has `RSVP_NOTIFICATION_FROM` and `CLOUDFLARE_EMAIL_API_TOKEN` blank, so real RSVP email sending still needs a verified wedding-domain sender and an Email Sending token unless Cloudflare already has a previously stored token. Attempted Cloudflare MCP authenticated readback, but `mcp__cloudflare_api.execute` returned Cloudflare error `10000: Authentication error`; re-check Pages custom domains, variables, DNS, and Email Sending after MCP credentials are refreshed.
- 2026-05-30: Updated RSVP dietary requirements and allergies from household-level fields to per-guest fields for both adults and children. The form now stores each adult as `{ name, dietaryRequirements, allergies }` and each child as `{ name, age, dietaryRequirements, allergies }` in the existing D1 JSON columns, with derived summary columns for CSV/admin/email compatibility and read-time normalisation for older rows that stored adult names as strings. The admin dashboard, guest confirmation, CSV export, and protected-preview E2E coverage now show per-guest dietary/allergy details. Verified with `node --check functions/_lib/rsvp.js`, `npm run check`, `npm run build`, `npm run test:rsvp:e2e` (7 passed), `npm run smoke:protected`, a focused Node validation smoke for new and legacy response shapes, and desktop/mobile RSVP screenshots saved to `tmp/rsvp-per-guest-desktop.png` and `tmp/rsvp-per-guest-mobile.png`.
- 2026-05-30: Restyled the RSVP page against `doc/plans/rsvp` as a visual reference while keeping the earlier removals: the form is now a single softened centred card, attendance options are larger side-by-side choice boxes, adult and child guests are grouped in one panel, RSVP update details are folded into the form, and the separate "What the RSVP asks"/"Updates" cards remain removed. Italian and German RSVP pages now use the same form treatment without the draft notice band. Added E2E coverage for multilingual RSVP field scope, absent notice/cards, and box alignment across English, Italian, and German. Verified with `npm run test:rsvp:e2e` and protected-preview Chrome screenshots saved to `tmp/rsvp-en-full.png`, `tmp/rsvp-it-full.png`, and `tmp/rsvp-de-full.png`; the in-app Browser connector was unavailable, so the screenshot pass used local Chrome through Playwright.
- 2026-05-30: Added `doc/plans/2026-05-30_cloudflare_hosting_rsvp_admin_instructions.md` with current Cloudflare MCP state, domain/hosting options including `gabyandmanfredi.wedding`, Cloudflare Pages custom-domain steps, production secret explanations, D1/admin operations, Email Sending setup, and a go-live checklist. Cloudflare MCP confirmed the Pages project, current `pages.dev` URL, D1 database, and that production currently exposes only `WEBSITE_PW`; Registrar availability check for the desired domain was not possible with the current Cloudflare API token.
- 2026-05-30: Updated the RSVP dietary requirements field from free text to a required dropdown with `None`, `Vegetarian`, `Vegan`, and `Other (specify)`. The conditional specify field is localised in English, Italian, and German and stores its custom text in the existing `dietary_requirements` D1 column, so no migration is needed. Verified with `node --check functions/_lib/rsvp.js`, `npm run check`, `npm run build`, and `npm run test:rsvp:e2e` (5 passed). A first concurrent RSVP E2E attempt failed because it overlapped with a separate `npm run build` writing `dist`; rerunning the E2E command by itself passed.
- 2026-05-30: Implemented the Cloudflare D1 RSVP system. Added D1 database `wedding_rsvp` with `rsvp_responses`, Pages Functions for `/api/rsvp` and `/api/admin/rsvp`, same-email supersession, multilingual RSVP UI, private admin dashboard, CSV export hardened against spreadsheet formula injection, delete flow, local protected-preview RSVP store, and Cloudflare Email Sending integration for both admin notification and guest confirmation. Site logout now also expires the separate admin cookie. Cloudflare MCP read-back confirmed D1 database `f4166d9c-3f0f-43ff-8c43-2806aa0adbc5` and the expected table/indexes. Verified with `node --check` on runtime scripts, `npm run check`, `npm run build`, `npm run test:rsvp:e2e` (5 passed), `npm run test:e2e -- --reporter=list --output=tmp/full-test-results` (128 passed, 40 RSVP specs intentionally skipped outside the protected-preview runner), `npm run smoke:protected`, and desktop/mobile RSVP/admin visual screenshots with zero horizontal overflow. Production still needs real Cloudflare Pages secrets plus a verified Email Sending sender before real email delivery.
- 2026-05-29: Captured the revised RSVP architecture in `doc/plans/2026-05-29_rsvp_cloudflare_d1_plan.md`: Cloudflare D1 as the system of record, multilingual in-site RSVP form, private admin dashboard, same-email supersession, no invitation codes, no travel/accommodation fields, notification email to `manfrediandgabriela@gmail.com`, and data retained until manual deletion. Read-only Cloudflare MCP/API check found existing Pages project `wedding-website` on `wedding-website-2ng.pages.dev` and no current D1 databases. No code or Cloudflare resources were changed.
- 2026-05-13: Added HTTP-level indexing protection by setting `X-Robots-Tag: noindex, nofollow` from the Cloudflare Pages middleware for pass-through static responses and redirects, with the same header mirrored in the local protected preview server. Extended protected smoke coverage to assert the header on the password gate and authenticated Schedule pages. Verified with `node --check` on the edited scripts, `npx tsc --noEmit --pretty false`, `npm run check`, `npm run build`, and `npm run smoke:protected`.
- 2026-05-13: Refined the logout and Schedule image placement follow-up: removed the visible box styling from the `Log out`/`Abmelden` button, removed the Stadthaus Zürich image from the vertical timeline, and placed it on the lower Civil ceremony card while keeping the Kirche St. Peter image on the Ceremony card. Verified with `npm run check`, `npm run build`, focused Schedule Playwright coverage, `npm run smoke:protected`, and a targeted mobile screenshot confirming Civil ceremony uses `stadthaus-zurich.png` while Ceremony uses `st-peter-zurich.jpg`.
- 2026-05-13: Updated the Swiss customs FAQ in English, Italian, and German to refer to customs checks and removed the QuickZoll/red-channel recommendation from the guest-facing answer while keeping the official customs links. Added a localised logout button to the authenticated header, with separate desktop and mobile placement so it does not sit inside the Menu or Language disclosures, and wired `/logout/` through both Cloudflare middleware and local protected preview to clear `gm_wedding_auth` and return guests to the password gate. Hardened Android verification to save screenshots into per-run folders and fail visibly if no local preview is running. Verified with `node --check` on the edited scripts, `npm run check`, `npm run build`, full `npm run test:e2e` (128 passed), `npm run smoke:protected`, protected-preview desktop/mobile screenshots, and `npm run test:android` on the connected `SM-S911B` Android device against a live local preview.
- 2026-05-13: Repaired the native Windows Git ACL drift that caused ordinary sandbox commits to fail with `.git/objects` permission errors. The fix normalised inherited Modify rights for `HP-MC\CodexSandboxOffline` and `HP-MC\CodexSandboxUsers` across `.git`, including entries that were owned by both the sandbox account and `HP-MC\Manfredi`. Verified with non-elevated Git object-write probes and a normal non-elevated commit.
- 2026-05-13: Added a Stadthaus Zürich photo to the private civil ceremony card under Schedule using the existing optional schedule-image shape, with localised English, Italian, and German alt text plus source and public credits. Stabilised the native Windows E2E path by replacing Playwright's `webServer` teardown with `scripts/run-e2e.mjs`, which builds, serves Astro preview on a dedicated local port, runs Playwright, and kills the spawned preview process tree; also capped diagnostic screenshots to avoid WebKit's 32767px full-page limit. Verified with `npm run check`, `npm run build`, focused Schedule Playwright coverage, full `npm run test:e2e` (128 passed), `npm run smoke:protected`, protected-preview desktop/mobile Schedule screenshots for English, Italian, and German, and `npm run test:android` on the connected `RFCW30AS8RE` Android device after unlocking it.
- 2026-05-12: Made the password auth cookie behave as the returning-visitor login state: after a valid password entry, the `gm_wedding_auth` cookie still uses a 30-day `Max-Age`, `HttpOnly`, and `SameSite=Lax`, and authenticated visits to `/` or `/welcome/` now redirect through to the requested `next` path or `/en/` instead of showing the password gate again. Mirrored the behaviour in the local protected-preview server and extended `scripts/smoke-protected-site.mjs` to assert the persistent cookie exists, is reused in a fresh browser context, and bypasses the root password page. Verified with `node --check` on protected-preview scripts, `npx tsc --noEmit --pretty false`, `npm run check`, `npm run build`, `npm run smoke:protected`, commit `851c081` pushed to GitHub, and network-approved `npm run smoke:live` against `https://wedding-website-2ng.pages.dev/`.
- 2026-05-12: Split the authenticated mobile header into two dropdowns: a separate `Language` disclosure and a `Menu` disclosure for page navigation only. Opening one now closes the other via native grouped `details` behaviour plus a small script fallback, and the protected smoke script asserts that the language switcher is no longer nested inside the mobile page menu. Verified with `npm run check`, `npm run build`, `npm run smoke:protected`, focused local mobile Menu/Language screenshots, commit `160050f` pushed to GitHub, network-approved `npm run smoke:live`, and focused live mobile checks/screenshots against `https://wedding-website-2ng.pages.dev/` showing no horizontal overflow and no menu overlap.
- 2026-05-12: Fixed mobile landing-frame distortion by replacing the stretched frame image with a fixed-slice frame treatment: `frame.svg` now supplies the fixed corners and verticals via `border-image`, while CSS draws the bottom horizontal strokes with a fixed centre gap around the ornament. The frame ornament now uses `object-fit: contain`, and the top date diamond sits in a true line break with a transparent background. Verified with `npm run check`, `npm run build`, `npm run smoke:protected`, focused local desktop/tablet/mobile/320px geometry checks and screenshots, commit `83e2ee3` pushed to GitHub, network-approved `npm run smoke:live`, and focused live desktop/mobile/320px checks showing the fixed 102px centre gap, sub-pixel ornament alignment, no broken images, and no horizontal overflow.
- 2026-05-12: Fixed bottom landing-frame ornament drift when the page is scaled: replaced the fixed bottom offset with a proportional offset tied to the `frame.svg` viewBox bottom-rim midpoint, keeping the ornament centred on the actual frame gap across desktop, mobile, and zoom-like scaled layouts. Verified with `npm run check`, `npm run build`, `npm run smoke:protected`, local multi-size/scale geometry checks plus screenshots, commit `c1e9601` pushed to GitHub, network-approved `npm run smoke:live`, and targeted live desktop/mobile/scale checks showing sub-pixel alignment, no broken images, and no horizontal overflow.
- 2026-05-12: Corrected the bottom landing-frame ornament treatment after visual review: kept the subtle layered landing background, cut a real centre gap into `public/images/landing/frame.svg`, and made the bottom frame ornament transparent/foreground instead of covering the rim with a painted patch. Verified with `npm run check`, `npm run build`, `npm run smoke:protected`, local desktop/mobile screenshots, commit `d2a717c` pushed to GitHub, network-approved `npm run smoke:live`, and targeted live desktop/mobile screenshots confirming the ornament sits on the rim, has no separate background block, no broken images, and no horizontal overflow.
- 2026-05-12: Switched the landing-page frame from CSS-drawn rules/corners to the provided `public/images/landing/frame.svg`, moved the ornament asset to `public/images/landing/ornament.svg`, and kept the three ornament placements unchanged. Allowed `/images/landing/` through both Cloudflare and local protected-preview middleware so the frame and ornaments load before password entry. Verified with `npm run check`, `npm run build`, `npm run smoke:protected`, focused local desktop/mobile welcome screenshots, commit `5fab587` pushed to GitHub, network-approved `npm run smoke:live`, and targeted live desktop/mobile screenshots confirming the frame image, two panel ornaments, one frame ornament, no old CSS corners, no broken images, and no horizontal overflow.
- 2026-05-12: Added the provided `doc/plans/ornament.svg` to the landing page: it now replaces the small password-frame top diamond, appears inverted below the password button, and sits in the interrupted bottom page frame beneath the language selector. Removed the active-language underline while preserving `aria-current`. Verified with `npm run check`, `npm run build`, `npm run smoke:protected`, local desktop/mobile welcome screenshots, commit `768f823` pushed to GitHub, network-approved `npm run smoke:live`, and targeted live desktop/mobile screenshots confirming two panel ornaments, one frame ornament, inverted lower ornament, no active underline, language bars, and no horizontal overflow.
- 2026-05-12: Matched the landing page more closely to `doc/plans/landing_page.png`: added the decorative double page margin with four corner accents and a bottom ornament, added an inset decorative border to the password frame, centred the composition, changed the language selector to `English | Italiano | Deutsch`, and replaced the emoji lock with a classic outline SVG lock. Verified with `npm run check`, `npm run build`, `npm run smoke:protected`, local desktop/mobile welcome screenshots, Cloudflare deployment of commit `3746146`, network-approved `npm run smoke:live`, and targeted live desktop/mobile landing screenshots confirming four corners, panel inset, SVG lock, language bars, and no horizontal overflow.
- 2026-05-12: Enabled visible language switching on the landing/password gate: the English, Italian, and German links now localise the gate prompt, accessible password label, placeholder, error message, submit button, active language state, and post-login destination while preserving the requested page path. Added E2E and protected smoke coverage for the language switch. Verified with `npm run check`, `npm run build`, targeted Chrome Playwright test (reported passing, runner hung during teardown), `npm run smoke:protected`, Cloudflare deployment of commit `6aa9118`, and network-approved `npm run smoke:live` against `https://wedding-website-2ng.pages.dev`.
- 2026-05-12: Applied the landing-page-only font approximation from `doc/plans/landing_page_font.css`: added bundled Cormorant Garamond and Montserrat web fonts, removed the welcome-page monogram, and kept the revised invitation password prompt. Verified with `npm run check`, `npm run build`, `npm run smoke:protected`, Cloudflare deployment of commit `0dcfb2e`, network-approved `npm run smoke:live`, and a targeted live browser check confirming no monogram plus the deployed title/prompt/language font families on `https://wedding-website-2ng.pages.dev/`.
- 2026-05-12: Refined the welcome/root password gate against `doc/plans/landing_page.png`: changed the prompt to "Please enter the password from your invitation.", stacked the G/M monogram, reduced the headline/date scale, and made the password panel typography and controls lighter. Verified with `npm run check`, `npm run build`, `npm run smoke:protected`, local protected-preview screenshot `tmp/welcome-gate-preview.png`, Cloudflare deployment of commit `6bd38df`, and network-approved `npm run smoke:live`.
- 2026-05-12: Fixed the deployed root URL landing behaviour: `/` now renders the same redesigned password gate as `/welcome/`, the gate is shared via `src/components/WelcomeGate.astro`, and Cloudflare/local protected-preview middleware no longer redirects authenticated visitors away from the root/welcome gate. Added a root gate E2E assertion and extended the protected smoke script to verify the authenticated root still shows the landing page. Verified with `npm run check`, `npm run build`, targeted Chrome Playwright assertions (passed before the known teardown hang), `npm run smoke:protected`, Cloudflare deployment of commit `403694c`, and network-approved `npm run smoke:live` against `https://wedding-website-2ng.pages.dev`.
- 2026-05-12: Applied the explicitly approved repo-local ACL repair for the native Windows workflow: granted `HP-MC\CodexSandboxOffline` Modify access on the write-heavy folders `.git`, `.astro`, `dist`, `node_modules`, `tmp`, `test-results`, and `playwright-report`, then applied the same grant directly to nested Vite/Astro cache folders that still blocked ordinary shell writes. Verified non-elevated write probes across generated/tooling folders, `npm run check`, and `npm run build`. Defender exclusions were not applied.
- 2026-05-12: Implemented the safe Windows workflow stabilisation pass from `doc/plans/windows_workflow_stabilisation_plan.md`: added reusable `smoke:protected` and `smoke:live` scripts backed by `scripts/smoke-protected-site.mjs`, documented the commands in `README.md`, and verified Chrome password-gated local/live checks without inline PowerShell here-strings. The initial audit found ordinary Codex shell writes still failed in generated/tooling folders while elevated writes succeeded; persistent ACL and Defender exclusion changes were deferred until separate explicit security approval. Verified with `node --check scripts/smoke-protected-site.mjs`, `npm run check`, `npm run build`, targeted Chrome Playwright tests, `npm run smoke:protected`, and `npm run smoke:live`.
- 2026-05-12: Completed local verification for item 7 from `doc/plans/2026-05-12_edits.md`: refreshed Italian and German edited sections by localising schedule timeline locations and the remaining Stay booking-card image alt text, then added E2E guards against English timeline fallback copy. Verified with `npm run check`, `npm run build`, rendered-output searches, targeted Chrome Playwright localisation coverage, and protected-preview Chrome checks/screenshots across Italian/German Schedule, Stay, Switzerland Guide, and FAQ on desktop and mobile.
- 2026-05-12: Completed local verification for item 6 from `doc/plans/2026-05-12_edits.md`: replaced the UK-only FAQ entry with a broader Switzerland visa/entry FAQ for EU/EFTA, UK, and US guests; added a separate Swiss customs FAQ covering the CHF 150 VAT-free limit, sensitive-goods allowances, EU/Northern Ireland versus Great Britain/USA animal-product constraints, QuickZoll/red-channel guidance, and the practical land-border strictness note. Recorded official source notes in `doc/assets/travel_research.md`. Verified locally with `npm run check`, `npm run build`, protected-preview Chrome login plus FAQ assertions/screenshots on desktop and mobile, and the targeted Chrome Playwright spec (reported passing, runner hung during teardown).
- 2026-05-12: Completed local verification for item 5 from `doc/plans/2026-05-12_edits.md`: replaced the Switzerland Guide ticket-machine image with a tightly cropped, frontal ZVV machine photo from `Ticket machine at Zurich Airport tram stop 03.jpg`, replaced the weather image with a colour Zurich rainy-day Unsplash photo, aligned the Public transport/Choosing tickets/Money and budgeting media panels, and added Swiss Half Fare Card visitor guidance in English, Italian, and German. Recorded source notes in `doc/assets/image_sources.md` and `doc/assets/travel_research.md`. Verified locally with `npm run check`, `npm run build`, protected-preview Chrome login plus Switzerland Guide assertions/screenshots on desktop and mobile, measured desktop title alignment at 0px spread, and targeted Chrome Playwright tests (both reported passing, runner hung during teardown).
- 2026-05-12: Completed local verification for item 4 from `doc/plans/2026-05-12_edits.md`: replaced the Kunsthaus Things to Do image with a cropped Wikimedia Commons `Heimplatz 2.jpeg` derivative that keeps the full Pipilotti Rist sculpture top visible and reduces the pavement-heavy foreground. Updated alt text, image credits, and source notes. Verified locally with `npm run check`, `npm run build`, protected-preview Chrome login plus Things to Do assertions/screenshots on desktop and mobile, and the targeted Chrome Playwright test (reported passing, runner hung during teardown).
- 2026-05-10: Repo contained `doc` only. Began scaffold from the build spec and global `.codex` instructions.
- 2026-05-10: Added local-network preview sharing via `bin/dev/share-local.ps1` and `SHARE_PREVIEW.md`.
- 2026-05-10: Expanded Things to Do content from `doc/plans/activity_ideas.md` with Zurich, lake/water, museum/chocolate, day-trip, and longer-trip cards.
- 2026-05-10: Applied `doc/plans/website_edits.txt` to expand the Switzerland Guide with ticket advice, child fares, Zürich Card, budgeting, car/taxi guidance, weather, and socket notes.
- 2026-05-10: Researched and revised travel guidance for Chicago, New York, London, and Sardinia, including EasyJet baggage caveats and Sardinia ferry-to-Genoa driving options.
- 2026-05-10: Replaced low-resolution Minted preview photos with original upload assets from the draft site's embedded photo data; visible couple/location photos are now mostly 1536-2048px on the long side.
- 2026-05-10: Replaced the Things to Do museums placeholder image with a Kunsthaus Zurich exterior photo from Wikimedia Commons and added the source to credits.
- 2026-05-11: Marked `C:/Projects/wedding-website` as a trusted Git directory with `safe.directory` so local Git tooling recognises the repo.
- 2026-05-11: Added the requested travel, accommodation, contact, and transport updates: `G & M` monogram, contact email, route-specific travel photos, Zurich/Kuesnacht venue map, Hotel Sonne/OXEN/Airbnb links, SBB Mobile guidance, airport train guidance, bike suggestion, and corrected child-fare wording.
- 2026-05-11: Refreshed Italian and German draft copy for the updated travel, accommodation, contact, and Switzerland guide pages. English remains the source language and translations still need fluent review.
- 2026-05-11: Added Playwright desktop/mobile E2E tests and a real Android Chrome screenshot check through ADB. Verified with `npm run check`, `npm run build`, `npm run test:e2e`, and `npm run test:android`; Android screenshots are written to `tmp/android`.
- 2026-05-11: Replaced the Sardinia travel card image with a scenic Ogliastra coast photo from Santa Maria Navarrese and removed the previous Cagliari card image.
- 2026-05-11: Added B & B Caffetino-Vino Richterswil as an accommodation option, with a note that it is a small B&B without a lift and guests should check the late return from Kuesnacht.
- 2026-05-11: Added train advice for reaching Kuesnacht ZH from Zurich HB, Zurich Airport, and Richterswil, with SBB Mobile/ZVV as the source of exact day-of connections.
- 2026-05-11: Added `/welcome/` as the password entry page and updated the Cloudflare middleware plus local protected preview server to use `WEBSITE_PW` from `env/website_pw.env` or deployment environment variables. `bin/dev/share-local.ps1` now shares the protected `/welcome/` preview.
- 2026-05-11: Added a FAQ entry explaining how to buy Swiss train tickets, with official SBB and ZVV links, and kept the answer localised in English, Italian, and German draft copy.
- 2026-05-11: Expanded Playwright E2E coverage to Chrome, Edge, Firefox, and WebKit/Safari-like engines across desktop and mobile/narrow viewports. Latest verification: `npm run check`, `npm run build`, `npm run test:e2e` (64 passed), and `npm run test:android` on the connected Android Chrome device.
- 2026-05-11: Linked the Switzerland Guide socket wording to the Wikipedia page for Swiss Type J/SN 441011 sockets, with E2E coverage for the English inline link.
- 2026-05-11: Added the Switzerland Guide exchange-rate widget under Money and budgeting, using the free open-source `fawazahmed0/exchange-api` data source via jsDelivr with a Cloudflare fallback. The widget shows CHF/USD, CHF/EUR, and CHF/GBP in both directions, and the Chicago travel guidance now suggests comparing New York layovers. Verified with `npm run check`, `npm run build`, `npm run test:e2e` (64 passed), a live Edge browser check against the real exchange-rate endpoint, and `npm run test:android` on the connected Android phone.
- 2026-05-11: Replaced the inaccurate Zurich wedding map schematic with an OpenStreetMap-derived SVG asset, retaining crisp SVG labels for Zurich Airport, Zurich HB, Kirche St. Peter, Hotel Sonne, Küsnacht, Altstetten, Oerlikon, and Richterswil. Updated English, Italian, and German map copy to mention Richterswil.
- 2026-05-11: Added Lake and water guidance for Oberer Letten and major Zurich lake badis, with official Stadt Zurich bathing links and a mobile screenshot check.
- 2026-05-11: Researched venue and accommodation accessibility, added Stay page guidance for mobility needs, and recorded source notes in doc/assets/accessibility_research.md.
- 2026-05-11: Polished Italian and German Stay page accessibility copy and repaired encoding damage in the localized Richterswil accommodation text.
- 2026-05-11: Replaced the Schedule reception card with a different official Hotel Sonne wide Festsaal ballroom photo and recorded the new image source.
- 2026-05-11: Replaced the skewed Chicago and London travel photos with straighter Wikimedia Commons images, using Tower Bridge for the UK card, and changed the Schedule reception card to a lake-facing Hotel Sonne view from Lake Zurich.
- 2026-05-11: Replaced the From Zurich Airport travel card image with a Wikimedia Commons photo of the Zürich Flughafen underground railway platforms.
- 2026-05-11: Renamed the Stay page Richterswil accommodation card from "Richterswil option" to "Richterswil" and added a Richterswil lakeside photo.
- 2026-05-11: Added the existing Kirche St. Peter photo to the Stay page Accessibility and mobility card.
- 2026-05-11: Replaced the Things to Do Local advice image with a distinct Lindenhof view of Zurich old town and the Limmat.
- 2026-05-11: Updated the Switzerland Guide imagery: moved the public-transport four-photo panel to the top of its card; added photos for ticket machines, Swiss francs, rainy Zurich, and personal recommendations; updated image credits and source tracking. Verified with `npm run check`, `npm run build`, `npm run test:e2e` (96 passed), and manual Playwright screenshots on desktop and mobile.
- 2026-05-11: Expanded Italian and German draft copy across the remaining pages, including Schedule, Things to Do, FAQ, RSVP, Gifts, Credits, home photo text, and Switzerland Guide gallery alt text. Updated UK travel wording in both languages and added E2E coverage to guard against English fallback copy. Verified with `npm run check`, `npm run build`, and `npm run test:e2e` (104 passed).
- 2026-05-11: Renamed the German Switzerland Guide label from "Schweiz-Guide" to "Hinweise zur Schweiz" in navigation, page content, and fallback copy. Verified with `npm run check`, `npm run build`, and `npm run test:e2e` (104 passed).
- 2026-05-11: Replaced the gloomier Kunsthaus Zurich image with a brighter Wikimedia Commons view showing the cafe terrace and updated attribution to Micha L. Rieser. Verified with `npm run check`, `npm run build`, `npm run test:e2e` (104 passed), and a manual Playwright screenshot of the Things to Do card.
- 2026-05-11: Removed Altstetten and Oerlikon from the Zurich wedding map while keeping them in the accommodation guidance text. Verified with `npm run check` and `npm run build`.
- 2026-05-11: Added a Wikimedia Commons lake-view photo of Küsnacht to the "By train to Küsnacht" travel section in English, Italian, and German. Verified with `npm run check`, `npm run build`, and targeted Playwright travel-page testing across 8 browser/mobile projects.
- 2026-05-11: Shortened the Zurich wedding map body copy in English, Italian, and German by removing the longer OpenStreetMap orientation sentence. Verified with `npm run check` and `npm run build`.
- 2026-05-11: Replaced the distorted Kunsthaus image with a bright, straight-on Wikimedia Commons photo of the Kunsthaus Zürich Chipperfield building and updated attribution. Verified with `npm run check`, `npm run build`, targeted Playwright coverage across 8 browser/mobile projects, and direct visual inspection of the selected asset.
- 2026-05-11: Removed the explicit "Minted draft" wording from the homepage photo-strip text. Verified with `npm run check` and `npm run build`.
- 2026-05-11: Removed remaining guest-facing Minted wording from the Italian and German homepage photo-strip text. Verified with `npm run check` and `npm run build`.
- 2026-05-11: Reworked the password landing page to match the provided formal paper-style visual: centred G & M monogram, names, date, password entry card, language destination links, and no public venue names. Verified with `npm run check`, `npm run build`, targeted Playwright coverage across 8 browser/mobile projects, desktop/mobile screenshots, and protected-preview password testing against `env/website_pw.env`.
- 2026-05-11: Adjusted the homepage Zurich hero background position so the foreground St Peter spire has more room at the top on desktop and mobile. Verified with `npm run check`, `npm run build`, targeted Playwright coverage across 8 browser/mobile projects, and rendered desktop/mobile screenshots.
- 2026-05-11: Implemented `doc/plans/website_refinement.md`: added `Küsnacht ZH` search guidance, SBB Mobile download reminders, Zurich Airport-to-Küsnacht ZH S16 guidance, UK/Luton/Basel/passport notes, and replaced the Swiss francs money-card image with the Lindenhof Zurich view. Verified with `npm run check`, `npm run build`, and Chrome desktop/mobile Playwright runs; Playwright reported all Chrome tests passing but the process hung during teardown. Android phone testing was attempted after reconnect, but local tunnel screenshots still showed Chrome `ERR_EMPTY_RESPONSE`; the live Pages URL currently shows the password gate and does not contain the unpushed refinement changes yet.
- 2026-05-12: Completed item 1 from `doc/plans/2026-05-12_edits.md`: refreshed the password landing page against `doc/plans/landing_page.png`, removed the "Private wedding website" wording from guest-facing chrome, kept venue names off the gate, and normalised root/login password flow to continue to `/en/`. Verified with `npm run check`, `npm run build`, targeted Chrome Playwright assertion (passed, runner hung during teardown), protected-preview password testing against `env/website_pw.env`, and desktop/mobile screenshots in `tmp/`.
- 2026-05-12: Completed item 2 from `doc/plans/2026-05-12_edits.md`: added the private civil ceremony at Stadthaus Zürich as the first schedule item, added explicit date/time metadata to the timeline, and updated English, Italian, and German schedule cards with the new TBD/date wording. Verified with `npm run check`, `npm run build`, protected-preview Chrome login plus Schedule assertions/screenshots on desktop and mobile, and targeted Chrome Playwright tests (passed, runner hung during teardown).
- 2026-05-12: Completed item 3 from `doc/plans/2026-05-12_edits.md`: updated Stay/FAQ car wording, replaced the Küsnacht late-night transport placeholder with current hourly-train guidance plus SBB Mobile caveat, and added S2/S8 guidance for Zürich HB to Richterswil in English, Italian, and German. Recorded source notes in `doc/assets/travel_research.md`. Verified with `npm run check`, `npm run build`, protected-preview Chrome login plus Stay/FAQ assertions/screenshots on desktop and mobile, and targeted Chrome Playwright tests (passed, runner hung during teardown).
